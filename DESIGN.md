# 《向前冲》 Spring Run — 设计说明

> 浏览器直接打开就能玩，手机竖屏优先（iPhone 16 Pro Max）。

## 1. 定位

Dino-runner 变体：自动向前跑，跳过/蹲过障碍，吃道具升级，越跑越快。
与 Chrome 恐龙不同的三点：
1. **不会一击死** — 3 颗心 + 无敌帧 + 每 3 关回血，失败也有温柔提示。
2. **收集俄语单词** — 收集西里尔字母拼出单词得奖励。
3. **一切都围绕兴趣主题** — 钢琴、vlog、旅行、牛肉面、跑步、网球。

## 2. 视觉：春日 + 夏日柔和粉

| 用途 | 色值 |
| --- | --- |
| 天空 上 / 下 | `#FFF1F7` → `#FFE0EF` |
| 云 / 光晕 | `#FFFFFF` / `#FFD6E8` |
| 远山 | `#EBD9F5` |
| 樱花树 / 花瓣 | `#FFC2DC` / `#FFB3D1` |
| 草地 | `#CFF3DE` → `#A9E6C6` |
| 主粉 | `#FF8FB8` |
| 深玫瑰（文字） | `#C4577F` |
| 薰衣草 | `#C9B6FF` |
| 柠檬 | `#FFE9A8` |
| 蜜桃 | `#FFC9A8` |
| 薄荷 | `#B8E8D0` |

天空会随关卡在「清晨 → 正午 → 午后 → 黄昏粉」之间缓慢过渡，让长时间游玩有变化。

## 3. 玩法

- **自动前进**，速度随等级上升（30 → 68 世界单位/秒）。
- **跳跃**：顶点 23 单位、滞空 0.735 秒。每一种障碍都算过「起跳窗口 vs 通过时间」，留了至少 0.14 秒余量，所以一定跳得过去（有测试守着）。
- **跳**：点屏幕任意处。**地面障碍和所有道具都靠跳**。
- **蹲**：按住右下角「蹲」按钮，或在屏幕上向下滑。**只用来钻过空中的障碍**，没有任何道具需要蹲着拿。空中下蹲会加速落地。
- **3 颗心**：撞击扣 1 心 + 1.4 秒无敌；每 3 级回 1 心（上限 3）。
- **分数**：距离（米）+ 道具加成。最高分存在 localStorage。

### 敌人（小麻烦）与障碍

地面（跳过）：钢琴、网球筐、行李箱、牛肉面碗、三脚架相机、跑鞋
空中（蹲过）：音符、飞来的网球、俄语书、小鸟

生成按「图案」成组，组间最小间距由当前速度反推（保证一定能过），并有自动化测试验证可通过性。
密度控制：障碍约 24 个/分钟、道具约 21 个/分钟，背景只保留少量装饰，屏幕上同时出现的东西一般不超过两三个。

### 道具

| 道具 | 效果 |
| --- | --- |
| ♥ 爱心 | 护盾（免一次伤害） |
| ♪ 音符 | +25 XP |
| 🍜 牛肉面 | +40 XP，回 1 心 |
| 西里尔字母 | +15 XP，拼出当前俄语单词（约 22 个/分钟，是最常见的道具；连续 5 个后会强制换别的） |

**所有道具都悬在半空（离地 13~19 单位），必须跳起来才吃得到** —— 站着不动一个也捡不到，也没法靠捡血躺着赢（有测试守着）。
道具落点会被检查：如果「跳起来吃它就会正好撞上障碍」，这个道具就不会生成。

**单词收集**：按顺序拼出 ПРИВЕТ / СПАСИБО / МОЛОДЕЦ / ЛЮБОВЬ 等 14 个词，集齐弹出一张单词卡（俄语 + 中文），额外 +150 XP，缺心时回满。

## 4. 升级与进度

- 距离 XP：0.5 XP / 单位；道具另有加成。
- 1→2 级需 120 XP，之后每级 +75 XP（3 分钟大约能到 10 级）。
- 升级时：屏幕闪光、关卡横幅、鼓励语、速度提升、1.5 秒无敌。
- 关卡名字是季节/爱好的彩蛋（樱花起跑、钢琴小径、牛肉面街、俄语课堂……）。

## 5. 鼓励语

分四类（升级 / 撞击 / 里程碑 / 结束），中文为主，穿插俄语，围绕这些兴趣主题：
「Молодец! 你真棒！」「这碗牛肉面是你的了 🍜」「镜头前的你最好看」「摔倒了也要漂亮地站起来」……

## 6. 技术

- 纯静态：`index.html` + `core.js`（纯逻辑，可在 Node 里测试）+ `game.js`（渲染/输入/音频）。
- 无依赖、无构建、无外部资源 → 直接丢到 GitHub Pages 就能跑，离线也能玩。
- Canvas 按 devicePixelRatio 缩放（上限 2.5，兼顾 iPhone 清晰度与性能）。
- 音效用 WebAudio 现场合成（钢琴音色琶音循环），不需要任何音频文件。
- 存档用 localStorage；分享用 navigator.share。
- 世界坐标用「高度 = 100 单位」的虚拟坐标系，任何屏幕比例都自适应。
- 控制：`env(safe-area-inset-*)`、`100dvh`、禁止双击缩放、禁止选中/长按菜单。

## 7. 验证

- `test/core.test.mjs`：物理/碰撞/升级/存档不变量 + 10 万帧随机输入模糊测试 + 「完美玩家」AI 通关模拟（证明每个关卡图案都能过）。
- `tools/verify.mjs`：Playwright + Chromium，iPhone 16 Pro Max 尺寸（440×956，DPR 3）跑 51 项端到端检查：控制台零错误、粉色调像素统计、每种障碍/道具的绘制对比度、触控跳跃高度、碰撞与下蹲、升级横幅、AI 长跑 20 秒不死、结算与存档、横屏、帧率、WebAudio。
- `tools/contrast-check.mjs`：量每个障碍在背景上的对比度（14×14 指纹最大色差）。
